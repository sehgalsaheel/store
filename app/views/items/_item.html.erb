    
    <tr>
        <td><%= item.name.titleize %></td>
        <td><%= item.thumbnail %></td>
        <td><%= number_to_currency(item.price) %></td>
        <td><%= item.category %></td>
        <td><a class="btn btn-default" <%= link_to(item.vendor.try(:name) || "NA", item.vendor, remote: true) %> </a> </td>
        <td><%= item.sort %></td>
        <td><%= link_to 'Edit', edit_item_path(item),  class: 'btn btn-primary btn-xs', remote: true %></td>
        <td><%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' },  class: 'btn btn-danger btn-xs', remote: true %></td>
    </tr>
    